<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://java.sun.com/xml/ns/javaee" xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_2_5.xsd" version="2.5">
  
  <!--  1. Start : 언어설정 -->
  <filter>
    <filter-name>encoding</filter-name>
    <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
    <init-param>
      <param-name>encoding</param-name>
      <param-value>UTF-8</param-value>
    </init-param>
  </filter>
  <filter-mapping>
    <filter-name>encoding</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>
  <!--  1. End -->
  
  <session-config>
    <session-timeout>60</session-timeout>
  </session-config>
  
  <!-- 2. Start 
   - 서버 실행시 불러올 파일들  -->
  <context-param>
    <param-name>contextConfigLocation</param-name>
    <param-value>/WEB-INF/config/root-config.xml</param-value>
  </context-param>
  <!--  2. End -->
  
  <!-- 3. Start
  - ContextLoaderListener와 DispatcherServlet은 WebApplicationContext 인스턴스를 생성하게 된다.
  - DispatcherServlet 이 생성한 인스턴스는 root Context 를 부모로 사용하는 자식 Context
  - 자식 Context 는 root Context 가 제공하는 Bean 사용 가능.
  - ContextLoaderListener 를 이용하여 공통 빈 설정.  
  -->
  <listener>
    	<listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
  </listener>
  
  <servlet>
    <servlet-name>action</servlet-name>
    <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
    <init-param>
      <param-name>contextConfigLocation</param-name>
      <param-value>/WEB-INF/config/servlet-context.xml</param-value>
    </init-param>
    <!-- 서블릿 최초 요청 들어올때 초기화 우선순위 -->
    <load-on-startup>1</load-on-startup>
  </servlet>
  
  <servlet-mapping>
    <servlet-name>action</servlet-name>
    <url-pattern>*.do</url-pattern>
  </servlet-mapping>
  
  <servlet-mapping>
    <servlet-name>action</servlet-name>
    <url-pattern>/</url-pattern>
  </servlet-mapping>
  
  <!--  3. End -->
  
</web-app>